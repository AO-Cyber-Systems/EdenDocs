# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-07-07)

**Core value:** Eden customers can collaboratively edit documents in the browser on infrastructure they control — privacy-first, with no third-party document cloud.
**Current focus:** Objectives 2 + 3 in parallel — Objective 2 (Rebrand) blocked-exogenous on upstream engine republish; Objective 3 (AOID OIDC) executing

## Current Position

Objective: 2 of 5 (EdenDocs Rebrand of the Web UI) — Objective 1 (From-Source Build Pipeline) complete, all 3 jobs done (01-01, 01-02, 01-03). Objective 3 (AOID Authentication Integration) running in PARALLEL per the roadmap's non-linear dependency note (depends only on Objective 1).
Job: 02-05 PARTIAL — Tasks 1-2 complete (checklist + mechanical gate live in CI); Task 3 (final green evidence) BLOCKED-EXOGENOUS on an upstream engine-tarball republish. Objective 3: 03-01/03-02/03-03/03-04 COMPLETE (waves 1-3 done, headless AUTH-01/02/04 e2e proof green — see 03-04-SUMMARY.md). 03-05 (wave 4, final) PARTIAL: Task 1 complete (runbook + README committed); Task 2 checkpoint:human-verify PENDING — real-AOID browser round trip (needs a reachable AOID instance + AOID client registration per docs/eden/WOPI-HOST.md runbook §4 "AOID client registration runbook"; user must see their AOID display name in the editor). AUTH-03 and objective success criteria 1/3 remain OPEN until that checkpoint passes — do not mark the objective complete on the fake-IdP evidence alone.
Status: Executing — Obj 2 waves 1+2 done, wave 3 (TRD 02-05) 2/3 tasks done pending upstream engine republish (resume action in 02-05-SUMMARY.md); Obj 3 waves 1-3 done, wave 4 (03-05) Task 1 done / Task 2 checkpoint PENDING (real-AOID human-verify)
Last activity: 2026-07-09 — TRD 03-05 Task 1 complete: authored `docs/eden/WOPI-HOST.md` (architecture/two-token model, quickstart, full EDENDOCS_WOPI_* env reference, AOID admin CreateClient registration runbook with one-time-secret capture warning, production `storage.wopi.alias_groups mode="groups"` trust config + proof-key/frame_ancestors notes, the four v1 limitations as documented decisions with v2 paths: IsAdminUser hard-false, no avatar claim, SupportsLocks=false, single-tenant issuer, port policy + public-repo/private-GOPRIVATE-dependency note) + `wopi-host/README.md` (30-line quickstart pointing at the runbook). Zero edits outside docs/eden/WOPI-HOST.md (new) and wopi-host/README.md (new) — TRADEMARK-MPL-CHECKLIST.md and all Objective-2 surfaces untouched. Verify gate passed (mode="groups", CreateClient, IsAdminUser, "exactly once", zero stray :8080 refs, README contains 8091). Commit: `bbc28d7ca63`. Task 2 (checkpoint:human-verify — a human, in a real browser, authenticating against a real AOID instance and seeing their AOID display name inside the editor) intentionally NOT executed this run per orchestrator directive: no services started, no AOID client registered, no simulated/faked pass, no "Verified against real AOID" line written, no 03-05-SUMMARY.md created (the TRD is intentionally left incomplete pending the real human-verify checkpoint). Previously: 2026-07-09 — TRD 03-01 complete (parallel workstream, worktree): scaffolded the additive `wopi-host/` Go module (own go.mod `github.com/AO-Cyber-Systems/EdenDocs/wopi-host`, go 1.26, eden-platform-go pinned `v0.0.0-20260708235425-c5fd1ee7cedb` + uuid v1.6.0, NO replace directive, NO vendoring). internal/config: `FromEnv()` with the EDENDOCS_WOPI_* env surface (ListenAddr default 127.0.0.1:8091, WopiTokenTTL 15m, RequireProof true), hard-fails any ":8080" listen addr ("port 8080 is banned project-wide; use 8091"), Config is a fmt.Stringer with always-redacted secrets (live-verified: real client secret set, log shows [REDACTED], grep finds nothing). internal/session (TDD RED→GREEN): Identity/WopiSession/Store — web login sessions + opaque WOPI access_token mint/lookup (`uuid.NewString()`, NOT a JWT — research Pitfall 2 two-credential boundary; `ExpiresAt time.Time` exposed so 03-03 computes access_token_ttl = UnixMilli), injected clock (no sleeps), sync.RWMutex maps with lazy expiry deletion, all 6 behavior cases green under `go test -race`. internal/storage (TDD RED→GREEN): filesystem store List/Stat/Read/Write with traversal guard (rejects /, \, .., empty across ALL methods → typed ErrBadFileID; ErrNotFound → HTTP 404 mapping for 03-03), atomic temp-in-DataDir+os.Rename writes, all 6 cases green incl. hello.odt binary round-trip. cmd/wopi-host: minimal main, `GET /healthz` live-verified HTTP 200 `{"status":"ok"}` on 127.0.0.1:8091 (full wiring lands 03-04). testdata/hello.odt byte-identical to upstream test/data/hello.odt (cmp). Gates: vet 0 / test -race 0 / build 0. ZERO files outside wopi-host/ across all 6 commits: `3969cd83823` (scaffold), `47238cc403b`/`296b31c256c` (session RED/GREEN), `3410ccdb176`/`cb097a26985` (storage RED/GREEN), `3a1e63ce447` (Stringer redaction fix). Two auto-fixes (Rule 1 test-arity compile error; Rule 2 secret-redacting Stringer). AUTH-01/AUTH-02 deliberately NOT marked complete — their requirement text spans 03-02/03-03/03-04 which also carry those IDs; last contributing TRD marks them. 03-02/03-03 build against the exported API surfaces recorded in 03-01-SUMMARY.md. Previously: 2026-07-08 — TRD 02-05 partial: authored `docs/eden/TRADEMARK-MPL-CHECKLIST.md` (6-class taxonomy, 13 per-surface rows, 21 justified exception rows, MPL byte-integrity section, BRAND-06 section, divergence allowlist now 5 upstream patch files) + `scripts/eden/trademark-exceptions.txt` (22 comment-free fixed strings from a full source-literal inventory) + `scripts/eden/verify-branding.sh` (8 gate groups: exceptions hygiene, BRAND-01..04 dist checks, per-occurrence ±40-char minification-safe BRAND-05 sweep scoped to upstream's production ship-set, legal-file `git diff --quiet HEAD` byte-integrity, BRAND-06 negative flag greps + coolwsd.xml canary) wired as one additive build.yml step after the smoke test. CI cycle 1 (run 28963503473) red on a gate-3 quoting assumption → `4771a289f99`; cycle 2 (run 28964158981) — gates 1-4 PASS on the real Linux dist and the sweep caught exactly ONE genuine leak, `<title>Collabora Online - Settings</title>` in adminIntegratorSettings.html.m4 → isolated upstream patch `6f9e184b4ec` (allowlist file #5). Cycle 3 (run 28964794754, both attempts) red at SMOKE, BEFORE the gate: upstream republished rolling `engine-main-assets.tar.gz` at 18:02:24Z (sha256 ea91ef33…→076a4129…) and the new engine SIGSEGVs kit startup (forkCOKit/lokit_main) so coolwsd never listens on 9980 — tree exonerated (the only commit in between is a one-line HTML title). Commits: `0b95f6ae237` (checklist+exceptions), `3f2147f91c0` (gate+CI step), `4771a289f99` (fix), `6f9e184b4ec` (title patch). ROADMAP 02-05 left unticked; BRAND-05 NOT marked complete. Resume: poll `gh api repos/CollaboraOnline/online/releases/tags/for-code-assets` for engine-main-assets.tar.gz updated_at > 2026-07-08T18:02:24Z, then `gh run rerun 28964794754`; on green fill checklist §8/Evidence, commit evidence, tick 02-05, mark BRAND-05. Previously: TRD 02-04 complete: wired the eden-branding/ assets into the from-source build entirely inside the two Eden-owned scripts (zero upstream files touched). `scripts/eden/build.sh` now configures with `--with-app-branding=$PWD/eden-branding --with-app-name="EdenDocs" --with-vendor="AO Cyber Systems" --with-help-url=https://aocyber.ai` (never any of the 4 phone-home flags — BRAND-06), asserts the Pitfall-1 `brandProductName` canary in generated coolwsd.xml, injects EdenDocs/https://aocyber.ai/eden-logo.svg into the brand keys via python3 re.sub, overlays repo-root favicon.ico (build-time only, NEVER committed — divergence-allowlist REMARK) and fully replaces browser/dist/welcome (rm -rf + cp -R), with loud dist assertions. `scripts/eden/smoke-test.sh` gained 6 runtime branding checks against live coolwsd on native 9980 (cool.html branding.css + `<title>EdenDocs</title>` + vendor string, favicon byte-compare, eden-logo.svg gold `efb32c`, Basic-Auth admin.html branding.js). CI: first run 28959752077 FAILED on a `curl | grep -q` pipefail EPIPE false negative (curl exit 23 on debug-mode cool.html; build side fully green) → fix cycle 1 converted all checks to fetch-to-file → run 28960901457 GREEN with `BRANDING SMOKE PASSED` + `SMOKE TEST PASSED` + POCO fallback line. The green vendor check empirically confirmed the AC_SUBST(VENDOR)→automake-preamble→m4 `-DVENDOR` chain. Commits: `252f98ece5a` (flags+canary+injection), `60daa2727fc` (overlays+smoke checks), `2bb08f0350f` (fetch-to-file fix). BRAND-01/02/03/04/06 marked complete; BRAND-05 closes in 02-05. Previously: TRD 02-03 complete: created the fully additive `eden-branding/` directory (11 new files, 3117 insertions / 0 deletions, zero upstream files touched, all on eden-main). The **real** AO gold-gradient emblem was downloaded verbatim from https://aocyber.ai/images/ao-icon.svg (76398 bytes; gradient stops `#fce88d` + `#efb32c` asserted present) and derived into `images/{eden-emblem,eden-logo,collabora-office-white,toolbar-bg-logo}.svg`, `welcome/eden-emblem.svg`, and a 3-layer (16/32/48) `favicon.ico`. `eden-logo.svg` nests the emblem as a `<svg>` (class-based gradients preserved) plus an "EdenDocs" gold wordmark. `branding.css` overrides all 7 primary-palette custom properties to the Eden gold ramp (#D4A853 gold-500 … `--color-primary-text` flipped to gold-950 #3D2A14 for WCAG), each carrying `!important` to survive the dark-theme runtime CSS injection; `--color-hyperlink` intentionally left upstream. `branding.js` sets `window.brandProductName = 'EdenDocs'` (sole admin-console producer) + `brandProductURL`. A Collabora-free self-contained welcome page (html+css+svg, source-availability link to github.com/AO-Cyber-Systems/EdenDocs) and a README documenting the intentional `collabora-office-white.svg` filename-collision overwrite mechanism round it out. Commits: `938f8bf3714` (assets), `160335432f3` (css+js), `25ce2fbc786` (welcome+README), `cd6a1d2da9f` (fix: drop "Collabora blue" from the shipped branding.css comment). Two Rule-1 auto-fixes: the English word "Collaborative" in the welcome lede and a "Collabora blue" css comment both tripped the mechanical zero-Collabora gate → reworded. BRAND-01/03/04 targeted but NOT marked complete — they close after TRD 02-04 wires these assets into the build. Test-gate note: the blanket collabora grep legitimately trips on README.md's required filename documentation; the narrowed (non-README) gate passes. Previously: 02-02 removed backstage/admin-Version-tab trademark strings (`f13d4afc092`, `b2759f21f4a`); 02-01 patched editor + admin `<title>` tags (`3aa5befe6c5`, `d27976178bb`); Objective 1 finished all 3 waves (CI GREEN on eden-main).

Progress: [███████░░░] 27%

## Accumulated Context

> Decisions and performance metrics are logged in STATE_ARCHIVE.md.

### Pending Todos

[From .planning/todos/pending/ — ideas captured during sessions]

None yet.

### Blockers/Concerns

- ~~Branch-tracking rule must precede Objective 2~~ **RESOLVED 2026-07-08** — recorded as a PROJECT.md Key Decision: `eden-main` tracks `upstream/main` (weekly-minimum merge cadence). Rationale: `main` is what `eden-main` already tracks and the only branch (besides `co-25.04`) publishing the `engine-*-assets.tar.gz` prebuilt tarball Objective 1's pipeline depends on. Objective 5 still formalizes the written workflow + divergence allowlist.
- ~~Eden brand tokens are TBD~~ **RESOLVED 2026-07-08** — recorded as a PROJECT.md Key Decision: Eden UI gold `#D4A853` ramp from the canonical Eden design system (`eden-ui-flutter` `lib/src/tokens/colors.dart` `EdenColors.gold`, 50–950 shades; primary "defaults to gold"), matching AO master-brand gold. Logo = official AO emblem (`https://aocyber.ai/images/ao-icon.svg`) + "EdenDocs" wordmark. Config/CSS-driven branding keeps a future palette swap cheap.
- **POCO tarball completeness unverified**: It's unverified whether `engine-main-assets.tar.gz` ships enough of `workdir/UnpackedTarball/poco/` for `--with-lo-builddir` to resolve POCO natively. Validate early in Objective 1 (per BUILD-02) rather than discovering it mid-CI-setup.
- ~~CheckFileInfo schema / AOID token model not cross-checked~~ **RESOLVED 2026-07-08** — `/devflow:research-objective 3` produced the full CheckFileInfo↔AOID claim mapping from source (coolwsd `wsd/wopi/WopiStorage.cpp` + AOID `internal/oauth/*`), in `.planning/objectives/03-aoid-authentication-integration-oidc/3-RESEARCH.md`. Key residuals now documented there as pitfalls: AOID has no avatar/picture claim (stock browser UI degrades gracefully to generic avatar — zero browser changes needed); AOID v1.0 hardcodes access-token `aud=["aoedge"]` so the WOPI host must skip the client-id audience check when decoding AOID access tokens for `ent[]`; `IsAdminUser` defaults to `false` in v1.
- **No prebuilt engine tarball for `distro/collabora/co-26.04`**: Only `main` and `co-25.04` currently publish `engine-*-assets.tar.gz`. If `eden-main` ever rebases onto a `co-26.04` distro branch, engine would need building from source until Collabora publishes a matching asset — factor into the Objective 5 branch-tracking decision.
- **Upstream engine-main-assets.tar.gz republished 2026-07-08T18:02:24Z breaks kit startup (SIGSEGV) against current eden-main** — blocks 02-05 Task 3 green evidence. New engine sha256 `076a41297c2ed659638617ed27cb6d4cff01f1558f36fb0e0b1abd6f4a14868e` crashes in forkCOKit/lokit_main (coolforkit-caps + libmergedlo.so) so coolwsd never accepts on 9980; prior sha256 `ea91ef33ba510eb1379568c58afe9ddc54aa05985f4ff963ab8e0a8cdfa6c59f` was smoke-green the same day. Tree exonerated (only a one-line HTML title changed between green and red). This VALIDATES the pre-existing unpinned-rolling-engine concern above. Resume = `gh run rerun 28964794754 --repo AO-Cyber-Systems/EdenDocs` after the next upstream republish (watch the asset's `updated_at` via `gh api repos/CollaboraOnline/online/releases/tags/for-code-assets`).

## Session Continuity

Last session: 2026-07-09
Stopped at: TRD 03-05 Task 1 complete (runbook + README committed); Task 2 checkpoint:human-verify PENDING — real-AOID browser round trip (needs a reachable AOID instance + AOID client registration per docs/eden/WOPI-HOST.md runbook §AOID client registration; user must see their AOID display name in the editor). Resume: /devflow:execute-objective 3 (continuation executes Task 2 checkpoint). Obj-2 residual unchanged: TRD 02-05 Task 3 BLOCKED-EXOGENOUS on upstream engine republish — resume action in 02-05-SUMMARY.md (rerun 28964794754 after engine-main-assets.tar.gz updated_at > 2026-07-08T18:02:24Z)
Resume file: .planning/objectives/03-aoid-authentication-integration-oidc/03-05-real-aoid-verification-TRD.md (no 03-05-SUMMARY.md yet — Task 2 checkpoint still pending)
